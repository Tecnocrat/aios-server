# AINLP.tachyonic[SECURITY] - Vault secrets must never be committed
# These files contain sensitive authentication tokens

# Vault credentials
vault-root-token.txt
vault-unseal-keys.json
vault-service-tokens.json

# Backup files
*.zip
*.bak

# Any token files
*-token*.txt
*-token*.json
