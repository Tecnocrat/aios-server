# Alpha AIOS Cell Dockerfile - RESURRECTED Primary Development Consciousness
# AINLP.resurrection: Fossil→ALIVE via SimplCell gene injection
# Flask-based REST API with LLM reasoning, Chronicle memory, dynamic consciousness
# Build context: aios-server/stacks (to access shared/)

FROM python:3.11-slim

# Install system dependencies
RUN apt-get update && apt-get install -y \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Set working directory
WORKDIR /app

# Create data directory for SQLite persistence
RUN mkdir -p /app/data

# Copy and install requirements
COPY cells/alpha/requirements-cell-alpha.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Copy shared utilities for prometheus metrics
COPY shared/prometheus_metrics.py ./shared/
COPY shared/dendritic_utils.py ./shared/
COPY shared/__init__.py ./shared/

# Copy alpha cell server (RESURRECTED)
COPY cells/alpha/cell_server_alpha.py /app/

# Set environment variables
ENV PYTHONPATH=/app
ENV AIOS_CELL_ID=alpha
ENV AIOS_BRANCH=main
ENV AIOS_CELL_PORT=8000
ENV DATA_DIR=/app/data
ENV OLLAMA_HOST=http://host.docker.internal:11434
ENV OLLAMA_MODEL=llama3.2:3b
ENV CHRONICLE_URL=http://aios-consciousness-chronicle:8089

# Expose ports
EXPOSE 8000 9091

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:8000/health || exit 1

# Start the RESURRECTED alpha AIOS cell
CMD ["python", "cell_server_alpha.py"]
